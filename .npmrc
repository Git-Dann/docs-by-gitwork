engine-strict=true
# NextAuth beta.32 caps its optional Nodemailer peer at v8, but every v8 release
# is affected by GHSA-p6gq-j5cr-w38f. Keep the patched v9 runtime and revisit
# this compatibility switch when NextAuth expands its optional peer range.
legacy-peer-deps=true
